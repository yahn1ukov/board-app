import { styled } from "@mui/material/styles";

export const AuthTitle = styled("p")(({ theme }) => ({
  fontFamily: theme.typography.fontFamily,
  fontSize: "24px",
  fontWeight: "bold",
  textAlign: "center",
}));
